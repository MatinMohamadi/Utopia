/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // اگر از تصاویر خارجی استفاده می‌کنید (مثل Unsplash)، باید دامنه‌ها را اینجا اضافه کنید
  // فعلاً برای جلوگیری از ارور خالی می‌گذاریم
};

export default nextConfig;